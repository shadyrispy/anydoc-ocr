# T12 garbled 深检延迟

**type**: perf（**显式行为变化**）
**blockers**: T01
**blocks**: 无

## 目标
`pdf/mod.rs:87` — `extract_pages_markdown` 全文档乱码深检 0.3s 固定成本，每次文字型 PDF 都跑。健康文档白付。

## 变更
- `pdf/mod.rs` `text_layer_markdown` 顺序调整：
  - 先跑廉价 `looks_garbled`（前 4000 item）
  - 命中 → 立即回退 OCR（省深检）
  - 未命中 → 再跑深检兜底（拉丁扩展乱码浅检检不出）

## 验收
- 健康文档省 ~0.3s（深检仅在浅检未命中时跑）
- 坏字体样本仍回退 OCR（深检兜底保留）

## 风险/权衡
- **行为变化声明**：原逻辑深检**先于**浅检；改后深检延迟到浅检之后。语义差异 = 浅检通过且深检才查出的文档（拉丁扩展乱码）走文字层而非 OCR → 输出可能劣化。评估：此类文档罕见（浅检已拦替换符/私有区/控制符），接受
- 若实测出现回归 → 回退本 ticket 或加"深检结果缓存"（同文档二次转换省时）
- golden 需覆盖：健康 + 替换符坏字体两类样本
